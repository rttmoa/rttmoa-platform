许多场景需要执行一些定时任务，例如：

	1.定时上报应用状态。
	2.定时从远程接口更新本地缓存。
	3.定时进行文件切割、临时文件删除。
	4.定时读取数据并处理、成功与失败的信息写入日志中

* * * * * *
┬ ┬ ┬ ┬ ┬ ┬
│ │ │ │ │ │
│ │ │ │ │ └─ 周日（0 - 7）（0 或 7 是周日）
│ │ │ │ └─── 月份（1 - 12）
│ │ │ └───── 日期（1 - 31）
│ │ └─────── 小时（0 - 23）
│ └───────── 分钟（0 - 59）
└─────────── 秒（0 - 59，可选）
